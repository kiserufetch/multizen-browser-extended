name: Release

# Triggered by pushing a tag like v0.2.1. The tag value should match the
# version in apps/desktop/package.json — electron-builder reads from there.
#
# Manual trigger via workflow_dispatch is also supported for testing the
# pipeline without creating a public release (it still uploads to a draft
# release that you can delete after).
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write # needed to create GitHub Releases

jobs:
  # Gate the (expensive, 3-OS) build matrix behind a strict typecheck so a
  # broken tag can never publish binaries.
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js 24
        uses: actions/setup-node@v5
        with:
          node-version: "24"
          package-manager-cache: false

      - name: Enable Corepack (Yarn 4)
        run: |
          corepack enable
          corepack prepare yarn@4.5.0 --activate

      - name: Cache Yarn
        uses: actions/cache@v5
        with:
          path: |
            ~/.yarn/berry/cache
            .yarn/cache
          key: yarn-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            yarn-${{ runner.os }}-

      - name: Install dependencies
        run: yarn install --immutable

      - name: Typecheck (all workspaces)
        run: yarn typecheck

  build:
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            script: build:mac:publish
          - os: windows-latest
            script: build:win:publish
          - os: ubuntu-latest
            script: build:linux:publish

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js 24
        uses: actions/setup-node@v5
        with:
          node-version: "24"
          # setup-node@v5 enables package-manager caching by default, which runs
          # the global Yarn 1.x before Corepack is activated and chokes on the
          # project's "packageManager": "yarn@4.5.0". We cache Yarn ourselves
          # below, so turn it off.
          package-manager-cache: false

      - name: Enable Corepack (Yarn 4)
        run: |
          corepack enable
          corepack prepare yarn@4.5.0 --activate

      - name: Cache Yarn
        uses: actions/cache@v5
        with:
          path: |
            ~/.yarn/berry/cache
            .yarn/cache
          key: yarn-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            yarn-${{ runner.os }}-

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build & publish
        env:
          # electron-builder picks this up to create the Release and
          # upload assets. Provided automatically by Actions.
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Skip mac code signing / notarization — open-source MVP runs
          # unsigned. Users get a Gatekeeper warning on first launch.
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: yarn workspace @multizen/desktop ${{ matrix.script }}
